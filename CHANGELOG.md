# 更新日志

本文件记录项目的版本变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [3.0.5] - 2026-08-31

一次集中的逻辑缺陷修复，同时调整了两处会影响用户既有系统设置的行为。

### 修复

- **深色模式在部分配置下永不生效**：浅色与深色起始时间相同时，判断式恒返回浅色。现在配置层会把
  深色起始时间纠正为 `(浅色起始 + 12) % 24`，函数层也做了同样的兜底。
- **配置解析失败会覆盖用户的计划任务时间**：配置文件损坏时程序会用默认值（6:00/18:00）继续运行并
  重建计划任务，一次临时性损坏就会把自定义时间永久冲掉。现在解析失败时跳过任务同步，原文件也不再被改写。
- **取消 UAC 提权提示被当作成功**：`ShellExecute` 的返回值无法区分"用户取消"，程序会静默退出、
  不给任何提示。改用 `ShellExecuteEx` 后可识别取消，并分别给出提示。
- **从命令行启动时会隐藏用户自己的终端窗口**：进程与宿主共享控制台，原先直接隐藏窗口会连带把
  cmd / PowerShell 藏起来。现在仅在独占控制台时隐藏。
- **界面刷新广播失败被误判为切换失败**：注册表其实已写入成功，只是部分窗口没即时重绘。现在降级为警告日志。
- **日志轮转失败被静默忽略**：`os.Remove` / `os.Rename` 的错误被丢弃，轮转失败时日志会无限增长。
  现在会记录失败原因，且不影响继续写入。
- **计划任务输出中文时日志乱码**：脚本现在显式设置输出编码为 UTF-8。
- **单个配置字段类型写错导致整份配置被丢弃**：时间字段改为容错解析，`6`、`"6"`、`6.0` 均可接受。
- **命令行参数只匹配第一个**：`ThemeSwitcher.exe --foo --scheduled` 现在也能正确进入定时分支。

### 行为变更

- **不再改写 `ColorPrevalence`**：此前会把该值强制写 0，等于静默关闭用户"在标题栏和任务栏显示强调色"
  的系统偏好且无法恢复。现在只管理 `AppsUseLightTheme` 与 `SystemUsesLightTheme`。
- **计划任务改为幂等注册**：把配置签名写入任务描述，一致时跳过注册。此前每次定时触发都会重新注册一次。

### 新增

- `--uninstall` 参数：删除计划任务后退出，卸载程序前可用。
- 配置被自动纠正时，日志会记录具体是哪些字段被纠正、纠正成了什么。
- `scheduler.Setup` 现在会校验小时参数范围，非法值不再拼进脚本。

## [3.0.4] - 2026-08-13

- 版本号更新至 3.0.4。

## [3.0.3] - 2026-08-04

- 修复：配置文件解析失败时保留原文件，不再被默认值覆盖。
- 修复：`runAsAdmin` 中命令行参数未做转义。

## [3.0.2] - 2026-08-04

- 重构：拆分为 config / logger / scheduler / theme 模块。
- 重构：改用 `golang.org/x/sys/windows/registry` 操作注册表。
- 修复：日志格式问题。

## [3.0.1] - 2026-08-04

- 与 3.0.2 指向同一提交。

## [3.0.0] - 2026-05-23

- 3.0 系列首个版本。

## 更早版本

- **2.8** (2026-02-21)、**2.0** (2026-02-20)、**1.5.0** (2026-01-13)、**1.0.0** (2026-01-07)：
  早期版本，当时的提交未留下详细记录，仅能从提交信息看出为功能上传与文档更新。
